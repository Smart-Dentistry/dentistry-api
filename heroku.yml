build:
  docker:
    web: Dockerfile.prod
run:
  web: gunicorn dentistry.wsgi:application
release:
  image: web
  command:
    - django-admin collectstatic --noinput --clear && django-admin migrate --noinput
