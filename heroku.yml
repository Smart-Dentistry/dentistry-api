build:
  docker:
    web: Dockerfile.prod
run:
  web: gunicorn dentistry.wsgi:application
release:
  image: web
  command:
    - python manage.py collectstatic --noinput --clear && django-admin migrate --noinput
